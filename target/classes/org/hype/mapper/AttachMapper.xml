<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
  "http://mybatis.org/dtd/mybatis-3-mapper.dtd">

<mapper namespace="org.hype.mapper.AttachMapper"> <!-- 정확한 Mapper 인터페이스 경로 -->

<!--     <insert id="imgInsert" parameterType="org.hype.domain.pImgVO">
        INSERT INTO pop_img_tbl (psNo, uuid, uploadPath, filename)
        VALUES (POPMAIN_SEQ.CURRVAL, #{uuid}, #{uploadPath}, #{filename})
    </insert> -->
	
<!-- 	<delete id="imgDelete" parameterType="org.hype.domain.pImgVO">
		DELETE FROM pop_img_tbl
    	WHERE psNo = #{psNo} AND uuid = #{uuid}
	</delete> -->

</mapper>	